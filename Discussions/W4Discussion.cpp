#include <iostream>
using namespace std;

int main() {
    int numberOfLoops;

    // Ask the user for the number of times to loop
    cout << "Enter the number of times to loop: ";
    cin >> numberOfLoops;

    // Loop the specified number of times
    for (int i = 1; j <= numberOfLoops; i++) {
        cout << "This is loop number: " << I << endl;
    }

    return 0;
}
